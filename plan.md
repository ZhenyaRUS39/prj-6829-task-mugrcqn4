# Plan: prj-6829-task-mugrcqn4

**Project:** `prj-3521`  
**Task ID:** `task-mugrcqn4`  
**Repo:** `prj-6829-task-mugrcqn4`  

## Summary

Декомпозиция: 4 этапа — инициализация Go-модуля и структуры репо, реализация TCP/WebSocket чат-сервера с broadcast, клиент и smoke-тесты, финальная документация и CI.

## Subtasks

### 1. Инициализировать Go-модуль и структуру проекта

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать репозиторий prj-6829-task-mugrcqn4, инициализировать go.mod (module github.com/ZhenyaRUS39/prj-6829-task-mugrcqn4, go 1.22), каркас каталогов cmd/server, cmd/client, internal/chat, README.md с описанием и инструкциями запуска, .gitignore (исключить bin/, vendor/, *.exe, .env). Зафиксировать выбор: TCP (net.Conn + горутины) или gorilla/websocket — выбрать TCP для простоты (без внешних зависимостей).
- **Test plan:** go build ./... проходит без ошибок; go.mod содержит module github.com/ZhenyaRUS39/prj-6829-task-mugrcqn4; структура cmd/server, cmd/client, internal/chat существует; README содержит секции Install и Usage.

### 2. Реализовать TCP-чат-сервер с broadcast

- **ID:** `sub-2`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** В internal/chat/server.go реализовать Hub (map[net.Conn]string, mutex, channel broadcast), Client.Read/Write-горутины, никнейм-протокол: первая строка клиента = NICK\n, далее MSG\n. Команды: /quit, /nick <name>, /list, /help. В cmd/server/main.go поднять listener на :9000 (или флаг -addr). Graceful shutdown по SIGINT/SIGTERM через context.Context.
- **Test plan:** go run ./cmd/server стартует без panic; go vet ./... без замечаний; ручной запуск двух nc-клиентов показывает, что сообщения одного видны другому; /list выводит список ников.
- **Dependencies:** `sub-1`

### 3. Реализовать TCP-клиент и юнит-тесты

- **ID:** `sub-3`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** В cmd/client/main.go сделать REPL-клиент (stdin -> сокет, сокет -> stdout), поддержку /-команд, флаги -addr и -nick. В internal/chat добавить юнит-тесты на Hub: TestHubBroadcast (2 клиента, 1 сообщение — оба получают), TestHubNickUnique (повторный ник отклоняется), TestHubRemoveOnDisconnect (закрытие conn удаляет из map). Покрытие core-логики >70%.
- **Test plan:** go test ./... -race проходит; go test -cover показывает покрытие internal/chat >70%; go run ./cmd/client -addr 127.0.0.1:9000 подключается к серверу и обменивается сообщениями.
- **Dependencies:** `sub-2`

### 4. Документация, Makefile и GitHub Actions CI

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Написать Makefile (build, run-server, run-client, test, lint, fmt), расширить README (архитектура, протокол, примеры curl/nc), добавить .github/workflows/ci.yml (jobs: test на ubuntu-latest, go 1.22, кеш модулей, go test -race -coverprofile). Прогнать локально make build && make test && make lint — всё зелёное. Вернуть diff origin/main..HEAD --stat координатору.
- **Test plan:** make build && make test && make lint завершаются с кодом 0; workflow ci.yml валиден (yamllint или actions-linter); README содержит секции Architecture, Protocol, Quickstart; git diff origin/main..HEAD --stat показывает только ожидаемые файлы.
- **Dependencies:** `sub-3`


---
*Generated by Hermes Orchestrator*
